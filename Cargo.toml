[workspace]
resolver = "2"
members = [
    "firewallx",
    "firewallx-common",
    "firewallx-ebpf",
]

[profile.release]
# Optimize the eBPF code
lto = true
opt-level = 3
panic = "abort"

[profile.dev]
panic = "abort"
