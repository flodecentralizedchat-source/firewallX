version: "3.8"
services:
  firewall:
    build: .
    privileged: true
    cap_add:
      - NET_ADMIN
      - SYS_ADMIN
      - BPF
    volumes:
      - /lib/modules:/lib/modules:ro
      - /sys/kernel/debug:/sys/kernel/debug:ro
    network_mode: "host"
    command: ["./target/release/firewallx"]
